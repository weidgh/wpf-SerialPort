<Window x:Class="demo1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="串口调试助手 (MVVM)" Height="760" Width="1180" Background="#F3F6FB">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D3DCEA"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="#2D7FF9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2D7FF9"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#C8D3E5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="330"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="串口配置">
                    <StackPanel>
                        <TextBlock Text="串口号"/>
                        <ComboBox ItemsSource="{Binding PortNames}" SelectedItem="{Binding SelectedPort}"/>

                        <Button Content="刷新串口" Command="{Binding RefreshPortsCommand}" Width="120"/>

                        <TextBlock Text="波特率"/>
                        <ComboBox ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}"/>

                        <TextBlock Text="校验位"/>
                        <ComboBox ItemsSource="{Binding ParityOptions}" SelectedItem="{Binding SelectedParity}"/>

                        <TextBlock Text="数据位"/>
                        <ComboBox ItemsSource="{Binding DataBitsOptions}" SelectedItem="{Binding SelectedDataBits}"/>

                        <TextBlock Text="停止位"/>
                        <ComboBox ItemsSource="{Binding StopBitsOptions}" SelectedItem="{Binding SelectedStopBits}"/>

                        <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                            <CheckBox Content="RTS" IsChecked="{Binding RtsEnabled}" Margin="0,0,20,0"/>
                            <CheckBox Content="DTR" IsChecked="{Binding DtrEnabled}"/>
                        </StackPanel>

                        <Button Content="{Binding TogglePortText}" Command="{Binding TogglePortCommand}" Height="34"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="接收配置">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <CheckBox Content="自动清空" IsChecked="{Binding AutoClearEnabled}" Margin="0,0,20,0"/>
                            <CheckBox Content="十六进制显示" IsChecked="{Binding ReceiveHexMode}"/>
                        </StackPanel>

                        <WrapPanel>
                            <Button Content="清空窗口" Command="{Binding ClearReceiveCommand}"/>
                            <Button Content="{Binding PauseButtonText}" Command="{Binding TogglePauseCommand}"/>
                            <Button Content="保存数据" Command="{Binding SaveDataCommand}"/>
                            <Button Content="保存路径" Command="{Binding SavePathCommand}"/>
                        </WrapPanel>

                        <TextBox Text="{Binding SaveDirectory, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="发送配置">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                            <CheckBox Content="自动发送" IsChecked="{Binding AutoSendEnabled}" Margin="0,0,12,0"/>
                            <TextBox Width="70" Text="{Binding AutoSendInterval, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,8"/>
                            <TextBlock Text="ms/次" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <CheckBox Content="十六进制发送" IsChecked="{Binding SendHexMode}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <WrapPanel>
                            <Button Content="手动发送" Command="{Binding SendCommand}"/>
                            <Button Content="清空发送" Command="{Binding ClearSendCommand}"/>
                            <Button Content="打开文件" Command="{Binding OpenFileCommand}"/>
                            <Button Content="发送文件" Command="{Binding SendFileCommand}"/>
                        </WrapPanel>

                        <TextBox Text="{Binding SendFilePath, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="170"/>
                <RowDefinition Height="44"/>
            </Grid.RowDefinitions>

            <GroupBox Header="显示区" Grid.Row="0">
                <TextBox Text="{Binding ReceiveText}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas"
                         Background="#F8FAFF"/>
            </GroupBox>

            <GroupBox Header="发送区" Grid.Row="2">
                <TextBox Text="{Binding SendText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas"/>
            </GroupBox>

            <StatusBar Grid.Row="3" Background="White">
                <StatusBarItem>
                    <TextBlock Text="状态:" FontWeight="SemiBold"/>
                </StatusBarItem>
                <StatusBarItem Width="330">
                    <TextBlock Text="{Binding StatusText}" TextTrimming="CharacterEllipsis"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock Text="发送计数:"/>
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Text="{Binding SendCount}"/>
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Text="接收计数:"/>
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Text="{Binding ReceiveCount}"/>
                </StatusBarItem>
                <StatusBarItem>
                    <Button Content="清空计数" Command="{Binding ClearCountCommand}" Margin="8,0,0,0" Padding="8,2"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
